__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.claude/
# NEVER commit credentials — a token committed here died 2026-08-21
# and took 18 days of RSVPs with it. Secrets live in Render env only.
.ghtoken
backend/.ghtoken
*.token

# local QA backups
*.bak_*
rsvp_data_BACKUP_*.json

# ephemeral local mirror written by the app; the real store is the repo root file
backend/rsvp_data.json
